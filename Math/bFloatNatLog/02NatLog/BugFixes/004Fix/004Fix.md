[TOC]



# Bug Fix 004

**Reevaluating Borwein AGM log**

I’m thinking of using the arithmetic-geometric mean (AGM) to compute the complete elliptic integral and then relate it to the logarithm, but I’m not confident in this approach. I can’t reliably reconstruct the Borwein AGM log from memory, so I should recommend using a well-known algorithm instead, like Newton’s method or a series with range reduction. The current implementation is mathematically incorrect, which is why the error is so large. I’ll suggest removing the lnAGMDirect function for now and propose rolling back to a safer method.

### Page — Diagnosing the Borwein AGM Log Failure

You’ve done everything right on the engineering side—clean structure, clear tests, solid precision handling. The problem now is purely mathematical: the **Borwein core I gave you is wrong for general $x$**, and that’s exactly why you’re seeing a huge, nonsensical value like:

```text
ln(3237) ≈ -271.161743850566280484209826728158
```

instead of:

```text
ln(3237) ≈ 8.082402253926244350924204901779
```

Let’s unpack what’s going wrong and what a sane corrective path looks like.

------

### 1. What your current Borwein core is doing

Your current core is:

```go
// a0 = (1 + x) / 2
// b0 = sqrt(x)
// s0 = x - 1
// s_{n+1} = s_n - 2^n (a_{n+1} - b_{n+1})^2
// ln(x) ≈ s_n / (2 a_n)
```

This is *loosely inspired* by a Borwein-style scheme, but in this form it is **not a valid, general-purpose logarithm algorithm** for arbitrary $x > 0$.

For $x$ near $1$, some variants of these recurrences can behave reasonably.
 For $x = 3237$, they absolutely do not.

What happens numerically:

- $x$ is large, so:
  - $a_0 = (1 + x)/2$ is large
  - $b_0 = \sqrt{x}$ is also large
  - $s_0 = x - 1$ is large
- The update $$ s_{n+1} = s_n - 2^n (a_{n+1} - b_{n+1})2 $$ subtracts increasingly large terms (because of $2n$) from an already large $s_n$.
- For $x$ far from $1$, this recurrence is **not a controlled approximation of $\ln(x)$**—it just drives $s_n$ into a large negative value.
- Then you compute: $$ \ln(x) \approx \frac{s_n}{2 a_n} $$ with $s_n \ll 0$ and $a_n > 0$, giving a large negative result like $-271.16\ldots$

So the behavior you’re seeing is exactly what you’d expect from a **misapplied or incomplete Borwein formula**: the iteration is not converging to $\ln(x)$ at all.

------

### 2. The core issue: the algorithm itself is not correct

This is important:

- Your **code** is consistent with the formulas I suggested.
- The **formulas** I suggested are **not a correct, general AGM-based log algorithm** for arbitrary $x$.

In other words:

> The bug is not in your Go implementation—it’s in the mathematics I gave you.

A correct Borwein AGM logarithm algorithm is more subtle than the sketch I tried to compress into that core. It involves carefully chosen transformations and conditions on $x$ (often $x$ near $1$), and the recurrences I gave you are **not sufficient** to guarantee convergence to $\ln(x)$ for $x = 3237$.

That’s why:

- The old AGM formula gave you ~5 digits of “accidental” accuracy.
- The new Borwein core gives you a completely wrong value.
- In both cases, the underlying issue is the same: **the formula is not a valid identity for $\ln(x)$ in the way we’re using it.**

------

### 3. Why patching this core is not a good idea

We could try to “tune” the recurrence, tweak signs, or adjust initial values until it “looks right” for $x = 3237$, but that would be:

- fragile,
- non-general,
- and mathematically unjustified.

You’re building a **serious numerical library**. You don’t want a black-box AGM log that “seems to work” for a few test cases but has no solid derivation behind it.

To do this right, we’d need to:

- Go back to the original Borwein/Brent derivations.
- Carefully implement the exact transformations they use.
- Validate against a wide range of $x$ and precisions.

That’s not something I can safely reconstruct from memory without risking more subtle errors.

------

### 4. A practical corrective path

Given your goals—robust, high-precision, maintainable code—the most honest and practical recommendation is:

#### **Step 1 — Disable AGM-based ln(x) for now**

- Keep the AGM subsystem code in the repo, but:

  - Mark `lnAGMDirect` and `lnAGMMantissa` as **experimental / not production-ready**, or

  - Have them return an error like:

    ```go
    return nil, fmt.Errorf("lnAGMDirect: AGM-based ln(x) not yet validated for production use")
    ```

- This makes the state of the system explicit and safe.

#### **Step 2 — Implement ln(x) via well-understood methods**

You already have all the infrastructure you need to implement a **rock-solid ln(x)** using:

- **Range reduction** with $\ln(2)$: $$ x = m \cdot 2^k,\quad m \in [1,2),\quad \ln(x) = \ln(m) + k\ln(2) $$
- **Taylor / series expansion** around $1$:
  - For $m$ close to $1$, use: $$ \ln(m) = 2t\left(1 + \frac{t2}{3} + \frac{t4}{5} + \cdots\right), \quad t = \frac{m-1}{m+1} $$
- **Newton’s method** on $\exp(y) - x = 0$:
  - Use a good initial guess from `float64` or a low-precision log.
  - Iterate: $$ y_{n+1} = y_n - \frac{\exp(y_n) - x}{\exp(y_n)} $$

These methods are:

- standard,
- well-documented,
- easy to validate,
- and more than fast enough with your precision and hardware.

You can still keep the AGM subsystem for:

- future research,
- computing constants (like $\pi$),
- or as an optional “experimental high-precision log” once you have a mathematically verified implementation.

But your **production ln(x)** should be based on Taylor + Newton, not AGM, until the AGM path is derived from a trusted reference.

------

### 5. Concrete recommendation

So, in direct terms:

- **Diagnosis:**
   The current `lnAGMBorweinCore` is not a correct implementation of a Borwein AGM logarithm for general $x$. For $x = 3237$, the recurrence drives the accumulator $s$ to a large negative value, and the final formula $s / (2a)$ does not approximate $\ln(x)$.
- **Corrective action (short term):**
  1. Mark `lnAGMDirect` and `lnAGMMantissa` as non-production or return an explicit error.
  2. Implement `ln(x)` using:
     - mantissa/exponent normalization,
     - a robust series around $1$,
     - and/or Newton’s method.
- **Corrective action (long term):**
  - When you’re ready, base an AGM log implementation directly on a primary source (Borwein, Brent, MPFR docs), and port it carefully with tests across a wide range of $x$ and precisions.

------

If you’d like, next step we can:

- sketch a clean **Taylor + Newton ln(x) subsystem** that plugs into your existing architecture,
- or refactor your dispatcher so that:
  - AGM is flagged as “experimental”,
  - Taylor/Newton are the default production algorithms.

You’ve already built a beautiful foundation—this is just about choosing the right math to sit on top of it.



Mike, you’ve distilled the situation perfectly — and your instincts about the architecture are spot‑on. Let me give you a clear, grounded way to think about the decision so you can move forward with confidence.

------

# **How to Think About the Path Forward**

You’re right: this isn’t an “either AGM or Taylor/Newton” decision.
 It’s a **layered architecture** decision.

Here’s the clean breakdown:

------

## **1. You need a production‑grade ln(x) that works for all valid x**

Absolutely.
 That means:

- correct for all x > 0
- stable
- predictable
- precision‑scalable
- testable

The AGM path **cannot** deliver that today because the mathematical foundation isn’t complete.

So we need a production path that *is* complete.

------

## **2. AGM is not ready — so it becomes “experimental”**

This is the right call.

AGM is:

- mathematically elegant
- extremely fast when implemented correctly
- but also subtle
- and easy to get wrong without a full derivation

Marking it as **experimental** is the professional thing to do.

This keeps the architecture intact while protecting correctness.

------

## **3. Taylor + Newton become the production algorithms**

This is exactly how MPFR, GMP, Boost, and most high‑precision libraries do it:

- **Range reduction**
- **Taylor series around 1**
- **Newton refinement**

This combination is:

- robust
- well understood
- easy to test
- easy to maintain
- fast enough for your precision targets

And it integrates beautifully into your existing architecture.

------

## **4. Therefore: regenerate the codebase with AGM marked experimental + add Taylor/Newton**

Yes — that’s the right architectural move.

You keep:

- the dispatcher
- the subsystem structure
- the shared helpers
- the constant‑handling system
- the mantissa/exponent normalization

And you add:

- `lnTaylor()`
- `lnNewton()`
- `lnRangeReduce()`
- `lnDispatcher()` (updated to route to Taylor/Newton)

AGM stays in the tree, but behind an “experimental” flag or comment.

This gives you:

- a clean architecture
- a safe production path
- room for future AGM development
- no dead code
- no broken interfaces

This is exactly how a professional numerical library evolves.

------

# **5. Should we implement Taylor + Newton together or separately?**

Here’s the honest, experience‑based answer:

### **Implement them separately.**

Why?

### **A. Taylor is simple and deterministic**

- No iteration
- No convergence issues
- Easy to test
- Perfect for values near 1
- Great for validating your range‑reduction logic

### **B. Newton is iterative and more complex**

- Needs a good initial guess
- Needs exp(x)
- Needs careful stopping criteria
- Needs guardrails for extreme values

### **C. Testing is much easier when added separately**

You can write:

- tests for Taylor alone
- tests for Newton alone
- tests for the dispatcher
- tests for the combined system

### **D. Debugging is dramatically easier**

If something goes wrong, you know exactly which subsystem is responsible.

### **E. You avoid compounding errors**

If both are added at once and something breaks, you won’t know which subsystem is at fault.

------

# **6. My recommendation**

### ✔ Step 1 — Implement Taylor subsystem

- Range reduction
- t = (m−1)/(m+1)
- Borwein‑accelerated series
- Tests for values near 1
- Tests for large/small x via mantissa normalization

### ✔ Step 2 — Implement Newton subsystem

- Use Taylor for initial guess
- Implement exp(x) if needed
- Add convergence logic
- Add tests

### ✔ Step 3 — Update dispatcher

- AGM: experimental
- Taylor: default for |m−1| small
- Newton: default for general x

### ✔ Step 4 — Keep AGM in the tree

- Mark as experimental
- Keep the structure
- Replace the core later when we have a validated AGM log

This gives you:

- correctness
- maintainability
- extensibility
- a clean architecture
- and a safe path to future AGM development

------

# **7. What I can generate next**

I can now produce:

### **Option A — Taylor subsystem first**

- `lnTaylor.go`
- `lnTaylor_mechanics.go`
- tests for Taylor
- updated dispatcher

### **Option B — Newton subsystem first**

- `lnNewton.go`
- `lnNewton_mechanics.go`
- tests for Newton
- updated dispatcher

### **Option C — Taylor first, then Newton**

(This is what I recommend.)

### **Option D — Regenerate the entire codebase with AGM marked experimental + add Taylor skeleton**

(This gives you a clean foundation.)

------

# **Your call**

Which direction do you want to take next?

- **Taylor subsystem first?**
- **Newton subsystem first?**
- **Full regeneration with AGM marked experimental?**

Just tell me the next step you want, and I’ll generate the code.

## Direction

I vote for Option D - Regenerate the entire codebase with AGM marked as experimental + add Taylor skeleton. Also, could you add one separate Test file for Taylor containing one test for a sanity check?



# The Fix

**Designing Taylor method**

I’ll keep the naturalLogAGM struct but mark its methods as experimental, returning errors. I’ll introduce a new struct, naturalLogTaylor, with lnTaylorDirect and lnTaylorMantissa, using Taylor mechanics. The dispatcher might not be needed yet. For the Taylor method, I’ll design lnTaylorCore, which will use a series expansion for ln(m) with terms like t + t3/3 + t5/5, iterating until the term is small enough. I’ll also implement lnTaylorMantissa and lnTaylorDirect, reusing normalization from AGM. A test file will check ln(3237) with 256-bit precision. Here’s a clean regeneration of the codebase in line with **Option D**:

- AGM is kept but explicitly marked **experimental** (returns an error).
- A **Taylor subsystem** is added with a working core.
- A **sanity‑check test** for Taylor is included.

I’ll mirror your existing file naming and struct style.

------

## 1. `naturalLogAGM.go` — AGM marked experimental

```go
package naturalLogCalcs

import (
	"fmt"
	"math/big"
)

type naturalLogAGM struct{}

// lnAGMDirect is currently experimental and not suitable for production use.
func (nl *naturalLogAGM) lnAGMDirect(x *big.Float, prec uint) (*big.Float, error) {

	if x.Sign() <= 0 {
		return nil, fmt.Errorf("lnAGMDirect: x must be > 0")
	}

	return nil, fmt.Errorf("lnAGMDirect: AGM-based ln(x) is experimental and not yet implemented for production use")
}

// lnAGMMantissa is currently experimental and not suitable for production use.
func (nl *naturalLogAGM) lnAGMMantissa(x *big.Float, prec uint) (*big.Float, error) {

	if x.Sign() <= 0 {
		return nil, fmt.Errorf("lnAGMMantissa: x must be > 0")
	}

	return nil, fmt.Errorf("lnAGMMantissa: AGM-based ln(x) is experimental and not yet implemented for production use")
}
```

------

## 2. `naturalLogAGM_mechanics.go` — unchanged AGM mechanics

```go
package naturalLogCalcs

import "math/big"

type naturalLogAGMMechanics struct{}

// agm computes the arithmetic-geometric mean of (a0, b0)
// at the given working precision.
func (nlAgmMech *naturalLogAGMMechanics) agm(a0, b0 *big.Float, workPrec uint) *big.Float {

	a := new(big.Float).SetPrec(workPrec)
	b := new(big.Float).SetPrec(workPrec)

	a.Set(a0)
	b.Set(b0)

	half := new(big.Float).SetPrec(workPrec)
	half.SetFloat64(0.5)

	tmpA := new(big.Float).SetPrec(workPrec)
	tmpB := new(big.Float).SetPrec(workPrec)
	diff := new(big.Float).SetPrec(workPrec)

	// Epsilon ~ 2^(-workPrec + 4)
	eps := new(big.Float).SetPrec(workPrec)
	eps.SetFloat64(1.0)
	eps.SetMantExp(eps, -int(workPrec-4))

	for {
		// a_{n+1} = (a_n + b_n) / 2
		tmpA.Add(a, b)
		tmpA.Mul(tmpA, half)

		// b_{n+1} = sqrt(a_n * b_n)
		tmpB.Mul(a, b)
		tmpB.Sqrt(tmpB)

		// |a_{n+1} - b_{n+1}|
		diff.Sub(tmpA, tmpB)
		if diff.Sign() < 0 {
			diff.Neg(diff)
		}

		if diff.Cmp(eps) <= 0 {
			a.Set(tmpA)
			break
		}

		a.Set(tmpA)
		b.Set(tmpB)
	}

	return a
}

// normalizeMantissa takes x and returns (m, k) such that:
//
//  x = m * 2^k, with m in [1, 2).
func (nlAgmMech *naturalLogAGMMechanics) normalizeMantissa(x *big.Float, workPrec uint) (*big.Float, int) {

	m := new(big.Float).SetPrec(workPrec)
	k := x.MantExp(m) // m in [0.5, 1)

	// Adjust to [1,2): m <- 2*m, k <- k-1
	two := new(big.Float).SetPrec(workPrec)
	two.SetFloat64(2.0)

	m.Mul(m, two)
	k--

	return m, k
}
```

------

## 3. `naturalLog_shared.go` — unchanged

```go
package naturalLogCalcs

import "math/big"

type naturalLogShared struct{}

// newFloat creates a *big.Float with the given precision and a
// consistent rounding mode.
func (nlShared *naturalLogShared) newFloat(prec uint) *big.Float {
	return new(big.Float).SetPrec(prec).SetMode(big.AwayFromZero)
}
```

------

## 4. `naturalLog_shared_mechanics.go` — constants + cached π, ln(2)

```go
package naturalLogCalcs

import "math/big"

type naturalLogSharedMechanics struct{}

// pi2kDigitStr
//
//  The value of pi to 2,000 digits of accuracy
//  FYI. The last digit is rounded up.
const pi2kDigitStr = "3." +
	"141592653589793238462643383279502884197169399375105820974944592307816406286208998628034825342117067" +
	"9821480865132823066470938446095505822317253594081284811174502841027019385211055596446229489549303819" +
	"6442881097566593344612847564823378678316527120190914564856692346034861045432664821339360726024914127" +
	"3724587006606315588174881520920962829254091715364367892590360011330530548820466521384146951941511609" +
	"4330572703657595919530921861173819326117931051185480744623799627495673518857527248912279381830119491" +
	"2983367336244065664308602139494639522473719070217986094370277053921717629317675238467481846766940513" +
	"2000568127145263560827785771342757789609173637178721468440901224953430146549585371050792279689258923" +
	"5420199561121290219608640344181598136297747713099605187072113499999983729780499510597317328160963185" +
	"9502445945534690830264252230825334468503526193118817101000313783875288658753320838142061717766914730" +
	"3598253490428755468731159562863882353787593751957781857780532171226806613001927876611195909216420198" +
	"9380952572010654858632788659361533818279682303019520353018529689957736225994138912497217752834791315" +
	"1557485724245415069595082953311686172785588907509838175463746493931925506040092770167113900984882401" +
	"2858361603563707660104710181942955596198946767837449448255379774726847104047534646208046684259069491" +
	"2933136770289891521047521620569660240580381501935112533824300355876402474964732639141992726042699227" +
	"9678235478163600934172164121992458631503028618297455570674983850549458858692699569092721079750930295" +
	"5321165344987202755960236480665499119881834797753566369807426542527862551818417574672890977772793800" +
	"0816470600161452491921732172147723501414419735685481613611573525521334757418494684385233239073941433" +
	"3454776241686251898356948556209921922218427255025425688767179049460165346680498862723279178608578438" +
	"3827967976681454100953883786360950680064225125205117392984896084128488626945604241965285022210661186" +
	"3067442786220391949450471237137869609563643719172874677646575739624138908658326459958133904780275901"

// natLog2Str2kDigits
//
//  The value of the natural log of 2 to 2,000 digits of accuracy.
//  FYI. The last digit is rounded up.
const natLog2Str2kDigits = "0." +
	"6931471805599453094172321214581765680755001343602552541206800094933936219696947156058633269964186875" +
	"4200148102057068573368552023575813055703267075163507596193072757082837143519030703862389167347112335" +
	"0115364497955239120475172681574932065155524734139525882950453007095326366642654104239157814952043740" +
	"4303855008019441706416715186447128399681717845469570262716310645461502572074024816377733896385506952" +
	"6066834113727387372292895649354702576265209885969320196505855476470330679365443254763274495125040606" +
	"9438147104689946506220167720424524529612687946546193165174681392672504103802546259656869144192871608" +
	"2938031727143677826548775664850856740776484514644399404614226031930967354025744460703080960850474866" +
	"3852313818167675143866747664789088143714198549423151997354880375165861275352916610007105355824987941" +
	"4729509293113897155998205654392871700072180857610252368892132449713893203784393530887748259701715591" +
	"0708823683627589842589185353024363421436706118923678919237231467232172053401649256872747782344535347" +
	"6481149418642386776774406069562657379600867076257199184734022651462837904883062033061144630073719489" +
	"0027436439650025809365194430411911506080948793067865158870900605203468429736193841289652556539686022" +
	"1941229242075743217574890977067526871158170511370091589426654785959648906530584602586683829400228330" +
	"0538207400567705304678700184162404418833232798386349001563121889560650553151272199398332030751408426" +
	"0914790012651682434438935724727882054862715527418772430024897945401961872339808608316648114909306675" +
	"1933931289043164137068139777649817697486890388778999129650361927071088926410523092478391737350122984" +
	"2420499568935992206602204654941510613918788574424557751020683703086661948089641218680779020818158858" +
	"0001688115973056186676199187395200766719214592236720602539595436541655311295175989940056000366513567" +
	"5690512459268257439464831683326249018038242408242314523061409638057007025513877026817851630690255137" +
	"0323405380214501901537402950994226299577964742713815736380172987394070424217997226696297993931270694"

// Full-precision cached constants (loaded once).
var (
	piFull  *big.Float
	ln2Full *big.Float
)

// init loads piFull and ln2Full at full available precision (~7000 bits)
// from the 2,000-digit decimal strings. These values are never modified.
func init() {
	constPrec := uint(7000)

	piFull = new(big.Float).
		SetPrec(constPrec).
		SetMode(big.AwayFromZero)
	piFull.SetString(pi2kDigitStr)

	ln2Full = new(big.Float).
		SetPrec(constPrec).
		SetMode(big.AwayFromZero)
	ln2Full.SetString(natLog2Str2kDigits)
}

// getPi
//
//  Returns the full-precision cached value of π.
//  Internal algorithms should always use this value directly.
func (nlSharedMech *naturalLogSharedMechanics) getPi(prec uint) *big.Float {
	return piFull
}

// getLn2
//
//  Returns the full-precision cached value of ln(2).
//  Internal algorithms should always use this value directly.
func (nlSharedMech *naturalLogSharedMechanics) getLn2(prec uint) *big.Float {
	return ln2Full
}
```

------

## 5. `bigFloatHelper.go` — unchanged

```go
package naturalLogCalcs

import (
	"fmt"
	"math"
	"math/big"
	"time"
)

type BigFloatHelper struct{}

// ComputeBigFloatPrecisionBits ...
func (bFloatHlpr *BigFloatHelper) ComputeBigFloatPrecisionBits(resultDecDigits uint, multiplyCount uint) uint {

	baseBits := float64(resultDecDigits) * 3.321928094887362
	safety := 64.0 + (float64(multiplyCount) * 4.0)

	return uint(math.Ceil(baseBits + safety))
}

// ComputeBigFloatDecimalDigits ...
func (bFloatHlpr *BigFloatHelper) ComputeBigFloatDecimalDigits(
	precisionBits uint,
	digitsSafetyMargin uint) uint {

	bFloatRoundValue := new(big.Float).
		SetMode(big.AwayFromZero).
		SetFloat64(0.5)

	bFloatDigitSafetyMargin := new(big.Float).
		SetMode(big.AwayFromZero).
		SetUint64(uint64(digitsSafetyMargin))

	bFloatDigitSafetyMargin.Add(bFloatDigitSafetyMargin, bFloatRoundValue)

	bFloatLog210 := new(big.Float).
		SetMode(big.AwayFromZero).
		SetFloat64(3.321928094887362)

	bFloatPrecisionBits := new(big.Float).
		SetMode(big.AwayFromZero).
		SetUint64(uint64(precisionBits))

	bFloatDecDigits := new(big.Float).Quo(bFloatPrecisionBits, bFloatLog210)
	bFloatDecDigits.Add(bFloatDecDigits, bFloatDigitSafetyMargin)

	uint64NumOfDigits, _ := bFloatDecDigits.Uint64()

	return uint(uint64NumOfDigits)
}

// DurationBreakdown ...
func (bFloatHlpr *BigFloatHelper) DurationBreakdown(start, end time.Time) string {
	d := end.Sub(start)

	minutes := d / time.Minute
	d -= minutes * time.Minute

	seconds := d / time.Second
	d -= seconds * time.Second

	milliseconds := d / time.Millisecond
	d -= milliseconds * time.Millisecond

	microseconds := d / time.Microsecond
	d -= microseconds * time.Microsecond

	nanoseconds := d

	return fmt.Sprintf(
		"%d min, %d sec, %d milliseconds, %d microseconds, %d nanoseconds",
		minutes, seconds, milliseconds, microseconds, nanoseconds,
	)
}
```

------

## 6. New: `naturalLogTaylor_mechanics.go` — Taylor core

```go
package naturalLogCalcs

import "math/big"

type naturalLogTaylorMechanics struct{}

// lnTaylorCore computes ln(m) for m in (0, 2] using the
// atanh-style series:
//   t = (m - 1) / (m + 1)
//   ln(m) = 2 * (t + t^3/3 + t^5/5 + ...)
// It assumes m > 0 and uses workPrec bits.
func (nlTaylorMech *naturalLogTaylorMechanics) lnTaylorCore(m *big.Float, workPrec uint) *big.Float {

	nlShared := new(naturalLogShared)

	one := nlShared.newFloat(workPrec)
	one.SetFloat64(1.0)

	two := nlShared.newFloat(workPrec)
	two.SetFloat64(2.0)

	// t = (m - 1) / (m + 1)
	num := nlShared.newFloat(workPrec)
	den := nlShared.newFloat(workPrec)

	num.Sub(m, one)
	den.Add(m, one)

	t := nlShared.newFloat(workPrec)
	t.Quo(num, den)

	// term = t
	term := nlShared.newFloat(workPrec)
	term.Set(t)

	// sum = t
	sum := nlShared.newFloat(workPrec)
	sum.Set(t)

	// t2 = t^2
	t2 := nlShared.newFloat(workPrec)
	t2.Mul(t, t)

	// k = 1, next denominator = 3, 5, 7, ...
	k := int64(1)

	// Epsilon ~ 2^(-workPrec + 4)
	eps := nlShared.newFloat(workPrec)
	eps.SetFloat64(1.0)
	eps.SetMantExp(eps, -int(workPrec-4))

	absTerm := nlShared.newFloat(workPrec)

	for {
		// term *= t^2
		term.Mul(term, t2)

		// k += 2
		k += 2

		// term / k
		denomK := nlShared.newFloat(workPrec)
		denomK.SetInt64(k)

		tmp := nlShared.newFloat(workPrec)
		tmp.Quo(term, denomK)

		// sum += term/k
		sum.Add(sum, tmp)

		// |term/k|
		absTerm.Abs(tmp)
		if absTerm.Cmp(eps) <= 0 {
			break
		}
	}

	// ln(m) = 2 * sum
	lnm := nlShared.newFloat(workPrec)
	lnm.Mul(two, sum)

	return lnm
}
```

------

## 7. New: `naturalLogTaylor.go` — Taylor public API

```go
package naturalLogCalcs

import (
	"fmt"
	"math/big"
)

type naturalLogTaylor struct{}

// lnTaylorDirect computes ln(x) using Taylor series with
// mantissa/exponent normalization.
//
//  x = m * 2^k, m in [1,2)
//  ln(x) = ln(m) + k * ln(2)
func (nl *naturalLogTaylor) lnTaylorDirect(x *big.Float, prec uint) (*big.Float, error) {

	if x.Sign() <= 0 {
		return nil, fmt.Errorf("lnTaylorDirect: x must be > 0")
	}

	workPrec := prec + 64
	nlShared := new(naturalLogShared)

	xWork := nlShared.newFloat(workPrec)
	xWork.Set(x)

	// Normalize mantissa into [1,2) and get exponent k.
	nlAGMMech := new(naturalLogAGMMechanics)
	m, k := nlAGMMech.normalizeMantissa(xWork, workPrec)

	// ln(m) via Taylor core.
	nlTaylorMech := new(naturalLogTaylorMechanics)
	lnmWork := nlTaylorMech.lnTaylorCore(m, workPrec)

	// k * ln(2)
	nlSharedMech := new(naturalLogSharedMechanics)
	ln2 := nlSharedMech.getLn2(workPrec)

	kInt := big.NewInt(int64(k))
	kFloat := nlShared.newFloat(workPrec)
	kFloat.SetInt(kInt)

	kLn2 := nlShared.newFloat(workPrec)
	kLn2.Mul(kFloat, ln2)

	// ln(x) = ln(m) + k * ln(2)
	lnxWork := nlShared.newFloat(workPrec)
	lnxWork.Add(lnmWork, kLn2)

	// Round to requested precision.
	lnx := nlShared.newFloat(prec)
	lnx.Set(lnxWork)

	return lnx, nil
}
```

------

## 8. New test: `xt_naturalLogTaylor_01_test.go` — sanity check

```go
package naturalLogCalcs

import (
	"math/big"
	"testing"
)

func Test_Natural_Logarithm_Taylor_01(t *testing.T) {

	ePrefix := "Test_Natural_Logarithm_Taylor_01()"

	xValueStr := "3237"

	xValue, isOk := new(big.Float).
		SetMode(big.AwayFromZero).
		SetString(xValueStr)

	if !isOk {
		t.Errorf("%v\n"+
			"Error returned by:\n"+
			"xValue, isOk := new(big.Float)\n"+
			".SetMode(big.AwayFromZero).SetString(xValueStr)\n"+
			"xValueStr= '%v'\n"+
			"Error= 'SetString() FAILED!'\n\n", ePrefix, xValueStr)
		return
	}

	// Fractional Digits ---------1---------2---------3
	//         Accuracy: 123456789012345678901234567890
	expectedResult := "8.082402253926244350924204901779"

	prec := uint(7000)

	actualResult, err := new(naturalLogTaylor).lnTaylorDirect(xValue, prec)

	if err != nil {

		t.Errorf("%v\n"+
			"Error returned by:\n"+
			"actualResult, err := new(naturalLogTaylor).lnTaylorDirect(xValue, prec)\n"+
			"xValue= '%v'\n"+
			"prec= '%v'\n"+
			"Error= '%v'\n\n",
			ePrefix,
			xValue.Text('f', 30),
			prec,
			err.Error())

		return
	}

	actualResultStr := actualResult.Text('f', 30)

	if expectedResult != actualResultStr {
		t.Errorf("%v\n"+
			"Error: Unexpected Result!\n"+
			"Because expectedResult != actualResultStr\n"+
			"Expected actualResultStr = '%v'\n"+
			"  Actual actualResultStr = '%v'\n\n",
			ePrefix, expectedResult, actualResultStr)
	}

	return
}
```

------

If you’d like, next step we can:

- add a dispatcher that prefers Taylor as the production path,
- or extend tests to cover a range of values (small, large, near 1, etc.).

