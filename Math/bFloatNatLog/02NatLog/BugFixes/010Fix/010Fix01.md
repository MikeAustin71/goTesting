[TOC]



# 010Fix01 - AGM Algorithm

## Question # 1



### Subject
This is a continuation of the conversation entitled 'Precision Loss in Go's NthRootGuess01'.


### Background:

I have uploaded two files containing final modified code for
the '09Fix'.

All 'go' source code is contained in the file
'go_source_code_Files.txt'.

All test source code is contained in the file
'All_Test_Source_Files.txt'.


### Summary:

The overall objective is to produce a working and testable
AGM algorithm source code implementation which will correctly
calculate natural logarithm values.

Previously, we had ceased development on the primary AGM
functions and focused instead the Taylor Series algorithms
which are now stable and functioning. The existing AGM functions
contained in this uploaded source code are listed as follows:

Source Code File: naturalLogAGM.go
Functions: 
  naturalLogAGM.lnAGMDirect()
  naturalLogAGM) lnAGMMantissa(

Source Code File: naturalLogAGM_mechanics.go
Functions:
  naturalLogAGMMechanics.normalizeMantissa()


### Tasks:

1. The next iteration of code modifications should be labeled as
'010Fix'

2. Please review the AGM algorithms source code.

3. Recommend a course of action designed to implement a working
   AGM algorithm in source code by using or modifying the existing
   structure. 
   
4. In generating a workable AGM algorithm, be careful not to disrupt,
   break or damage the existing Taylor Series algorithm functions.   

5. Generate one new test file containing tests which can be used to 
   validate the AGM algorithm results. In configuring these tests please
   use the 'expected values' generated in the Taylor series test files. 
   These 'expected values' have been verified and can therefore be used
   to correctly validate AGM calculation results.

### Response Format

Please format your response to this request in user friendly Typora markdown
using the following formatting guidelines.
- “Use Typora-friendly Markdown with headings, lists, and code blocks.”
- “Prepare this Page as a Markdown document.”
- "Prepare this Page as a separate 'Page'"
- Please format all math using Typora‑compatible delimiters:
  - Inline math: $ ... $
  - Display math blocks: $$ ... $$
  - Code Blocks

2. If you produce Microsoft Power Point files for task # 3, please
provide these files separately in a format you think most suitable
for transferring these Microsoft Power Point files to me for my use.

Thanks!



## Question #2

I don't understand your response. I don't see a separate page, I don't see an AGM plan, and I don't see the requested AGM test file. Please elaborate.

### Response

Thanks for your patience, Mike — and thank you for calling out the missing pieces. You’re absolutely right: you asked for **a separate Page**, **an AGM implementation plan**, and **a new AGM test file**, and none of that appeared. Let’s fix that cleanly and completely.

Below is **the full, separate Page** you requested — written in Typora‑friendly Markdown, containing:

1. **A clear AGM implementation plan (010Fix)**
2. **A review of your existing AGM source code**
3. **A recommended path to a correct AGM algorithm without breaking Taylor**
4. **A new AGM test file**, using the verified Taylor expected values
5. **All math in Typora‑compatible delimiters**

This is the complete deliverable you asked for.

